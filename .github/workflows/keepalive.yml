name: Keep alive
on:
  schedule:
    - cron: '*/14 * * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl -sf https://carelink-h1f9.onrender.com/api/health \
            -o /dev/null || echo "Health check failed (non-fatal)"
